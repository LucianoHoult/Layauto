# 当前工作入口

更新：2026-09-09。PM 维护当前节点和恢复入口；详细状态、决定、证据与 reviewer 结论归任务文件。

## 当前节点

**W001 首版全局规划已验收，可以继续 W002 的只读资料调查与方案准备。** 2026-09-09 用户要求核对另一会话 review 并判断能否继续；PM 已接收 0 项实质 findings 的独立 review、核对实际版本与要求覆盖。随后用户授权 commit 并 push：规划原稿 `8482b68` 与集成记录 `156e62d` 已推送 GitHub main，远端 hash 已核对。用户已将 W002 提示词发给执行会话；执行进展和产物待回传核验。

- [路线图](roadmap.md)：M0 准入准备 → M1 no_change 保真文件闭环 → M2 首个实际 shrink 闭环 → M3 多器件/多 delta 首版覆盖 → M4 可复现验收；路线与实质验收要求保持已审内容。
- [W001 — 首版全局规划](tasks/W001-v2-global-plan.md)：已验收（规划文档），详细 review 与 PM 接收事实在任务文件；原稿已提交本地 main，集成身份见下文。
- **当前执行交接：[W002 — 首个输入证据与闭环准入准备](tasks/W002-first-input-admission.md)，已由用户派发。** 先从仓库既有材料形成准入矩阵、真实资料缺项、共享合同/失败流及 M1 近期实现包草案；真实资料不足仍可开展独立工程准备，不能称真实准入已通过。
- 无未处理 finding 或新的产品范围待决项。真实 tech/model/query profile、Stage 1 live acquisition、必要物理 operator/rule 条件仍待 W002 查明；v2 产品里程碑均未验收。Stage 6 signoff 延期不免除输入义务。
- 当前授权允许提交现有已验收规划及配套集成记录；保持 **不开始产品实现、不拆分架构正文**。W002 新增执行产物仍不 commit；M2–M4 尚无已确认日历排期。

## 恢复与版本身份

先读根 [AGENTS](../AGENTS.md)、[项目 README](../README.md)、[协作规则](collaboration/rules.md)，再读 [W001 PM 接收](tasks/W001-v2-global-plan.md#6-pm-验收与交接pm)、W002 和相关架构原文。下一角色建议为 **另一会话执行 W002 的调查/文档准备**，本会话继续 PM 的全局协调、review 组织与验收；完整启动 prompt 在 W001 交接段。用户已向另一执行会话发送启动 prompt；本 PM 未新建会话，尚未核验其实际执行状态。为避免与执行会话并发改写，本轮不修改 W002 文件；其中旧的“尚未派发/启动”是上次交接记录，后续由 PM 收到实际回传后更新。

**GitHub 已发布版本：`156e62d150349ac127476074f73524677f6d7760`，包含已验收原稿 `8482b68f6b2b0958f1ac501b15cd393a66782c4a`。** 七文件逐项与已验收快照一致后提交，包含 roadmap、导航、W001 独立 review/PM 接收与 W002 要求。原 `6eccdff` 仅为架构和审阅起始基线，不能作为 W002 执行 checkout（尚无任务文件）。

提交后的授权/集成记录随本页另行保存，当前恢复版本可用 `git log -1 --format=%H -- docs/current-work.md` 定位。下一执行会话核对 HEAD 包含上述原稿及当前恢复记录，再读 W002；默认串行使用同一 checkout，避免 PM 与执行者同时改任务文件。跨主机先取得相同提交；已用 `git ls-remote --heads origin refs/heads/main` 核对远端发布的 `156e62d`。本页后续状态记录不改变原稿与任务要求。

原待审与 PM 接收快照保留为历史证据，路径与 hash 见 [W001](tasks/W001-v2-global-plan.md#验收版本集成与恢复)；日常恢复使用 Git 版本，不依赖本机临时目录。原规划、review 增补和 PM 状态更新仍可分开追溯。

本次完成规划的独立会话恢复→review→PM 接收，不代表产品执行→修复→组合验收周期已演练。W002 调查中的新关键范围/格式/物理解释/验收变化，应带具体样本、依据、影响和推荐项上报用户；普通技术细化与 findings 依项目规范处理。

## W002 下一审查节点

顺序：**执行会话交付 → PM 核对范围/版本/证据完整性 → 针对实际交付发独立方案 review prompt → findings 处理与复核 → PM 验收**。目前没有 W002 已完成产物与待审快照，不启动审查来推测或认可尚未交付的方案。

执行完成后需能定位任务执行段、实际附件/样本来源、被审 commit 或包含 untracked 的完整快照，以及未验证/缺证/待决项。PM 核对这些材料后准备具体审查范围；重点为 synthetic 与真实准入的证据分界、共享 owner/publication/失败流、物理 operator/rule/repair 依赖，以及 M1 近期包的可执行验收。Reviewer 只写 W002 的 reviewer 段，关键合同方案通过 review 后才进入后续实现包。

## 前一节点的已验收记录

最小协作规范已在 2026-09-08 独立 review 后验收并集成，原稿版本为 `e57f2ddd3122892ad73f473803f2d16d2f632e87`。包含完整 review、56 个本地链接/1 个锚点检查、包配置与 wheel 限制的历史入口保存在 `6eccdff99a4da64e46921d16a4c339198813bc78:docs/current-work.md`，可用 `git show 6eccdff:docs/current-work.md` 恢复。

该前一节点只证明协作规范交付；当前规划的独立 review 与 PM 接收见 W001，二者均不证明任何 v2 产品、真实工具/PDK 或生产正确性。现行规则、模板和环境文档继续保持各自职责；旧工作流提案的未采用部分仍只作参考。
